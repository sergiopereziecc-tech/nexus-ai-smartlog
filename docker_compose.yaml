version: '3.8'

services:
  db:
    image: postgres:17-alpine
    container_name: nexus-db
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}